name: Sandbox Test

on:
  pull_request:
    paths:
      - "sandbox/**"
  push:
    branches: [main]
    paths:
      - "sandbox/**"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: sandbox/test.sh
